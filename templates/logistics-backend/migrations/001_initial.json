{
  "statements": [
    {
      "sql": "CREATE TABLE public.shipment (id uuid PRIMARY KEY, origin text NOT NULL, destination text NOT NULL, picked_up_at timestamptz, delivered_at timestamptz, weight_kg numeric(10,2) NOT NULL, hazmat boolean NOT NULL DEFAULT false)",
      "severity": "safe"
    },
    {
      "sql": "CREATE TABLE public.vehicle (id uuid PRIMARY KEY, vin text NOT NULL UNIQUE, type text NOT NULL, capacity_kg numeric(10,2) NOT NULL, current_driver_id uuid)",
      "severity": "safe"
    },
    {
      "sql": "CREATE TABLE public.driver_log (id uuid PRIMARY KEY, driver_id uuid NOT NULL, vehicle_id uuid NOT NULL, status text NOT NULL, recorded_at timestamptz NOT NULL DEFAULT now(), location_lat numeric(9,6), location_lon numeric(9,6))",
      "severity": "safe"
    },
    {
      "sql": "CREATE INDEX shipment_status_idx ON public.shipment (delivered_at, picked_up_at)",
      "severity": "safe"
    },
    {
      "sql": "CREATE INDEX driver_log_driver_idx ON public.driver_log (driver_id, recorded_at DESC)",
      "severity": "safe"
    }
  ],
  "envKeys": [
    "DATABASE_URL",
    "DATABASE_URL_READONLY"
  ],
  "recommendedTargetUrlKey": "DATABASE_URL",
  "destructiveCount": 0,
  "riskyCount": 0
}