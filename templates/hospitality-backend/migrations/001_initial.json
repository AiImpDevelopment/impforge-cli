{
  "statements": [
    {
      "sql": "CREATE TABLE public.property (id uuid PRIMARY KEY, name text NOT NULL, address text NOT NULL, room_count integer NOT NULL, opened_at timestamptz NOT NULL DEFAULT now())",
      "severity": "safe"
    },
    {
      "sql": "CREATE TABLE public.reservation (id uuid PRIMARY KEY, property_id uuid NOT NULL, guest_id uuid NOT NULL, check_in date NOT NULL, check_out date NOT NULL, total_cents bigint NOT NULL, status text NOT NULL DEFAULT 'confirmed')",
      "severity": "safe"
    },
    {
      "sql": "CREATE TABLE public.room_inventory (id uuid PRIMARY KEY, property_id uuid NOT NULL, room_type text NOT NULL, available_date date NOT NULL, available_count integer NOT NULL)",
      "severity": "safe"
    },
    {
      "sql": "CREATE INDEX reservation_property_idx ON public.reservation (property_id, check_in)",
      "severity": "safe"
    },
    {
      "sql": "CREATE INDEX room_inventory_property_idx ON public.room_inventory (property_id, available_date)",
      "severity": "safe"
    }
  ],
  "envKeys": [
    "DATABASE_URL",
    "DATABASE_URL_READONLY"
  ],
  "recommendedTargetUrlKey": "DATABASE_URL",
  "destructiveCount": 0,
  "riskyCount": 0
}