{
  "statements": [
    {
      "sql": "CREATE TABLE public.field (id uuid PRIMARY KEY, farm_id uuid NOT NULL, name text NOT NULL, hectares numeric(10,2) NOT NULL, soil_type text)",
      "severity": "safe"
    },
    {
      "sql": "CREATE TABLE public.crop_lot (id uuid PRIMARY KEY, field_id uuid NOT NULL, crop_type text NOT NULL, planted_at timestamptz NOT NULL DEFAULT now(), harvested_at timestamptz, lot_code text NOT NULL UNIQUE)",
      "severity": "safe"
    },
    {
      "sql": "CREATE TABLE public.pesticide_application (id uuid PRIMARY KEY, field_id uuid NOT NULL, product_epa_reg text NOT NULL, applied_at timestamptz NOT NULL DEFAULT now(), rate_per_hectare numeric(10,4) NOT NULL, applicator_id uuid NOT NULL)",
      "severity": "safe"
    },
    {
      "sql": "CREATE INDEX field_farm_idx ON public.field (farm_id, name)",
      "severity": "safe"
    },
    {
      "sql": "CREATE INDEX crop_lot_field_idx ON public.crop_lot (field_id, planted_at DESC)",
      "severity": "safe"
    }
  ],
  "envKeys": [
    "DATABASE_URL",
    "DATABASE_URL_READONLY"
  ],
  "recommendedTargetUrlKey": "DATABASE_URL",
  "destructiveCount": 0,
  "riskyCount": 0
}